define("epi/debounce", [
    "dgrid/util/misc"
], function (misc) {

    /*=====
    return function (cb, context, delay) {
        // summary:
        //      Returns a function which calls the given callback only after a
        //      certain time has passed without successive calls.
        // tags:
        //      public
    };
    =====*/

    return misc.debounce;
});
